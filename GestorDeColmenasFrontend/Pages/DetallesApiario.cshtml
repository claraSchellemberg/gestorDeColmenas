@page "{id}"
@using GestorDeColmenasFrontend.Modelos
@model GestorDeColmenasFrontend.Pages.DetallesApiarioModel
@{
    Layout = null;
}

<!DOCTYPE html>
<html class="light" lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Detalles Apiario - @Model.Apiario?.Nombre</title>

    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries,typography"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700;900&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" rel="stylesheet" />
    <script>
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        "primary": "#FFC107",
                        "background-light": "#F8F9FA",
                        "background-dark": "#1e293b",
                        "card-light": "#FFFFFF",
                        "card-dark": "#334155",
                        "text-light": "#343A40",
                        "text-dark": "#e2e8f0",
                        "text-muted": "#6B7280",
                        "border-light": "#e5e7eb",
                        "border-dark": "#475569",
                        "accent-honey": "#E69B00",
                        "accent-green": "#4CAF50",
                        "accent-blue": "#2196F3",
                        "accent-red": "#F44336",
                        "accent-orange": "#FF9800",
                    },
                    fontFamily: { "display": ["Inter", "sans-serif"] },
                    borderRadius: { "DEFAULT": "0.5rem", "lg": "0.75rem", "xl": "1rem", "full": "9999px" }
                }
            }
        };
    </script>
    <style>
        .material-symbols-outlined {
            font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
        }
    </style>
</head>
<body class="font-display bg-background-light dark:bg-background-dark text-text-light dark:text-text-dark">
    <div class="flex h-screen w-full flex-col lg:flex-row">

        <!-- ========== SIDEBAR ESCRITORIO (lg+) ========== -->
        <aside class="hidden lg:flex w-64 flex-col border-r border-border-light dark:border-border-dark bg-card-light dark:bg-card-dark p-4">
            <div class="flex items-center gap-3 px-2 mb-6">
                <img src="https://via.placeholder.com/150" alt="Avatar" class="h-10 w-10 rounded-full object-cover" />
                <div class="flex flex-col">
                    <span class="text-base font-medium">Usuario</span>
                    <span class="text-text-muted text-sm">email@ejemplo.com</span>
                </div>
            </div>

            <nav class="flex flex-col gap-2">
                <a class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-black/5 dark:hover:bg-white/5 text-text-muted" href="/PerfilUsuario">
                    <span class="material-symbols-outlined">person</span>
                    <span class="text-sm font-medium">Mi Perfil</span>
                </a>
                <a class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-black/5 dark:hover:bg-white/5 text-text-muted" href="/Dashboard">
                    <span class="material-symbols-outlined">dashboard</span>
                    <span class="text-sm font-medium">Dashboard</span>
                </a>
                <a class="flex items-center gap-3 px-3 py-2 rounded-lg bg-primary/20 text-text-light dark:text-text-dark font-medium" href="/ListadoApiarios">
                    <span class="material-symbols-outlined">hive</span>
                    <span class="text-sm">Apiarios</span>
                </a>
                <a class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-black/5 dark:hover:bg-white/5 text-text-muted" href="/ListadoColmenas">
                    <span class="material-symbols-outlined">grid_view</span>
                    <span class="text-sm font-medium">Colmenas</span>
                </a>
                <a class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-black/5 dark:hover:bg-white/5 text-text-muted" href="#">
                    <span class="material-symbols-outlined">description</span>
                    <span class="text-sm font-medium">Registros</span>
                </a>
            </nav>

            <div class="mt-auto">
                <a class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-black/5 dark:hover:bg-white/5 text-text-muted" href="#">
                    <span class="material-symbols-outlined">logout</span>
                    <span class="text-sm font-medium">Cerrar Sesión</span>
                </a>
            </div>
        </aside>

        <!-- ========== CONTENIDO PRINCIPAL ========== -->
        <div class="flex-1 flex flex-col min-h-screen lg:min-h-0">

            <!-- Header tablet -->
            <header class="hidden md:flex lg:hidden items-center justify-between border-b border-border-light dark:border-border-dark bg-card-light dark:bg-card-dark px-6 py-3 shadow-sm">
                <div class="flex items-center gap-4">
                    <a href="/ListadoApiarios" class="p-2 -ml-2 hover:bg-gray-100 dark:hover:bg-slate-700 rounded-lg">
                        <span class="material-symbols-outlined">arrow_back</span>
                    </a>
                    <h1 class="text-lg font-bold">Detalles del Apiario</h1>
                </div>
                <div class="flex items-center gap-4">
                    <button class="relative rounded-full p-2 hover:bg-gray-100 dark:hover:bg-slate-700">
                        <span class="material-symbols-outlined">notifications</span>
                    </button>
                    <a href="/PerfilUsuario">
                        <div class="bg-center bg-no-repeat aspect-square bg-cover rounded-full size-10 hover:ring-2 hover:ring-primary transition-all"
                             style="background-image: url('https://via.placeholder.com/150');"></div>
                    </a>
                </div>
            </header>

            <!-- Header móvil -->
            <header class="md:hidden flex items-center justify-between p-4 border-b border-border-light dark:border-border-dark bg-card-light dark:bg-card-dark">
                <a href="/ListadoApiarios" class="p-2 -ml-2">
                    <span class="material-symbols-outlined">arrow_back</span>
                </a>
                <h1 class="text-xl font-bold">@Model.Apiario?.Nombre</h1>
                <div class="w-10"></div>
            </header>

            <!-- Contenido -->
            <main class="flex-1 overflow-y-auto p-4 md:p-8">
                <div class="mx-auto max-w-7xl">

                    <!-- Información del Apiario -->
                    <div class="mb-6">
                        <h2 class="hidden md:block text-3xl lg:text-4xl font-black tracking-tight">@Model.Apiario?.Nombre</h2>
                        
                        <!-- Detalles del Apiario -->
                        <div class="mt-4 bg-card-light dark:bg-card-dark rounded-lg shadow-sm border border-border-light dark:border-border-dark p-6">
                            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                                <div class="flex items-center gap-3">
                                    <span class="material-symbols-outlined text-primary">location_on</span>
                                    <div>
                                        <p class="text-xs text-text-muted">Ubicación</p>
                                        <p class="font-medium">@(Model.Apiario?.UbicacionDeReferencia ?? "Sin ubicación")</p>
                                    </div>
                                </div>
                                <div class="flex items-center gap-3">
                                    <span class="material-symbols-outlined text-primary">my_location</span>
                                    <div>
                                        <p class="text-xs text-text-muted">Coordenadas</p>
                                        <p class="font-medium">@(Model.Apiario?.Latitud ?? "-"), @(Model.Apiario?.Longitud ?? "-")</p>
                                    </div>
                                </div>
                                <div class="flex items-center gap-3">
                                    <span class="material-symbols-outlined text-primary">calendar_today</span>
                                    <div>
                                        <p class="text-xs text-text-muted">Fecha de Alta</p>
                                        <p class="font-medium">@Model.Apiario?.FechaAlta.ToString("dd/MM/yyyy")</p>
                                    </div>
                                </div>
                                <div class="flex items-center gap-3">
                                    <span class="material-symbols-outlined text-primary">grid_view</span>
                                    <div>
                                        <p class="text-xs text-text-muted">Total Colmenas</p>
                                        <p class="font-medium">@(Model.Apiario?.Colmenas?.Count ?? 0)</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Título Colmenas -->
                    <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-4 mb-4">
                        <h3 class="text-xl font-bold">Colmenas del Apiario</h3>
                    </div>

                    <!-- ========== VISTA MÓVIL: CARDS ========== -->
                    <div class="md:hidden space-y-4">
                        @if (Model.Apiario?.Colmenas != null && Model.Apiario.Colmenas.Any())
                        {
                            @foreach (var colmena in Model.Apiario.Colmenas)
                            {
                                var (bgColor, textColor, dotColor) = GetStatusColors(colmena.Estado);
                                <div class="bg-card-light dark:bg-card-dark rounded-xl shadow-md border border-border-light dark:border-border-dark overflow-hidden">
                                    <div class="p-4">
                                        <div class="flex justify-between items-start">
                                            <div>
                                                <h2 class="text-lg font-bold">Colmena @colmena.Nombre</h2>
                                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium @bgColor @textColor mt-1">
                                                    <span class="w-2 h-2 mr-1.5 @dotColor rounded-full"></span>
                                                    @GetEstadoTexto(colmena.Estado)
                                                </span>
                                            </div>
                                            <a href="/DetalleColmena/@colmena.Id" class="text-gray-400 hover:text-primary">
                                                <span class="material-symbols-outlined">visibility</span>
                                            </a>
                                        </div>
                                        <div class="mt-4 border-t border-border-light dark:border-border-dark pt-4 space-y-3">
                                            <div class="flex items-center text-sm">
                                                <span class="material-symbols-outlined text-lg mr-2 text-gray-500">calendar_today</span>
                                                <span class="font-medium">Instalación sensores:</span>
                                                <span class="ml-auto">@colmena.FechaInstalacionSensores.ToString("dd/MM/yyyy")</span>
                                            </div>
                                            @if (!string.IsNullOrEmpty(colmena.Descripcion))
                                            {
                                                <div class="flex items-center text-sm">
                                                    <span class="material-symbols-outlined text-lg mr-2 text-gray-500">description</span>
                                                    <span class="text-text-muted">@colmena.Descripcion</span>
                                                </div>
                                            }
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                        else
                        {
                            <div class="text-center py-8 text-text-muted">
                                <span class="material-symbols-outlined text-4xl mb-2">grid_view</span>
                                <p>No hay colmenas en este apiario</p>
                            </div>
                        }
                    </div>

                    <!-- ========== VISTA TABLET/ESCRITORIO: TABLA ========== -->
                    <div class="hidden md:block bg-card-light dark:bg-card-dark rounded-lg shadow-sm border border-border-light dark:border-border-dark">
                        <div class="overflow-x-auto">
                            <table class="w-full text-sm text-left text-gray-500 dark:text-gray-400">
                                <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-slate-700 dark:text-gray-300">
                                    <tr>
                                        <th class="px-6 py-3" scope="col">Colmena</th>
                                        <th class="px-6 py-3" scope="col">Descripción</th>
                                        <th class="px-6 py-3 text-center" scope="col">Fecha Instalación</th>
                                        <th class="px-6 py-3 text-center" scope="col">Cuadros</th>
                                        <th class="px-6 py-3 text-center" scope="col">Estado</th>
                                        <th class="px-6 py-3" scope="col"><span class="sr-only">Acciones</span></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (Model.Apiario?.Colmenas != null && Model.Apiario.Colmenas.Any())
                                    {
                                        var index = 0;
                                        @foreach (var colmena in Model.Apiario.Colmenas)
                                        {
                                            var rowBg = index % 2 == 0 ? "bg-card-light dark:bg-card-dark" : "bg-gray-50 dark:bg-slate-800";
                                            var (bgColor, textColor, dotColor) = GetStatusColors(colmena.Estado);
                                            <tr class="@rowBg border-b dark:border-border-dark hover:bg-gray-100 dark:hover:bg-slate-700/50">
                                                <th class="px-6 py-4 font-semibold text-text-light dark:text-text-dark whitespace-nowrap" scope="row">
                                                    <a class="hover:text-primary hover:underline" href="/DetalleColmena/@colmena.Id">@colmena.Nombre</a>
                                                </th>
                                                <td class="px-6 py-4 text-text-muted max-w-xs truncate">
                                                    @(colmena.Descripcion ?? "-")
                                                </td>
                                                <td class="px-6 py-4 text-center">
                                                    @colmena.FechaInstalacionSensores.ToString("dd/MM/yyyy")
                                                </td>
                                                <td class="px-6 py-4 text-center">
                                                    @(colmena.Cuadros?.Count ?? 0)
                                                </td>
                                                <td class="px-6 py-4 text-center">
                                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium @bgColor @textColor">
                                                        <span class="w-2 h-2 mr-1.5 @dotColor rounded-full"></span>
                                                        @GetEstadoTexto(colmena.Estado)
                                                    </span>
                                                </td>
                                                <td class="px-6 py-4 text-right">
                                                    <a class="font-medium text-primary hover:underline" href="/DetalleColmena/@colmena.Id">Ver</a>
                                                </td>
                                            </tr>
                                            index++;
                                        }
                                    }
                                    else
                                    {
                                        <tr>
                                            <td colspan="6" class="px-6 py-8 text-center text-text-muted">
                                                <span class="material-symbols-outlined text-4xl mb-2">grid_view</span>
                                                <p>No hay colmenas en este apiario</p>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </main>

            <!-- Navegación móvil -->
            <nav class="md:hidden sticky bottom-0 bg-card-light dark:bg-card-dark border-t border-border-light dark:border-border-dark shadow-lg">
                <div class="flex justify-around p-2">
                    <a class="flex flex-col items-center justify-center p-2 rounded-lg text-text-muted hover:bg-gray-100" href="/PerfilUsuario">
                        <span class="material-symbols-outlined">person</span>
                        <span class="text-xs mt-1">Perfil</span>
                    </a>
                    <a class="flex flex-col items-center justify-center p-2 rounded-lg text-text-muted hover:bg-gray-100" href="/Dashboard">
                        <span class="material-symbols-outlined">dashboard</span>
                        <span class="text-xs mt-1">Dashboard</span>
                    </a>
                    <a class="flex flex-col items-center justify-center p-2 rounded-lg text-primary" href="/ListadoApiarios">
                        <span class="material-symbols-outlined">hive</span>
                        <span class="text-xs mt-1 font-bold">Apiarios</span>
                    </a>
                    <a class="flex flex-col items-center justify-center p-2 rounded-lg text-text-muted hover:bg-gray-100" href="/ListadoColmenas">
                        <span class="material-symbols-outlined">grid_view</span>
                        <span class="text-xs mt-1">Colmenas</span>
                    </a>
                </div>
            </nav>
        </div>
    </div>
</body>
</html>

@functions {
    (string bgColor, string textColor, string dotColor) GetStatusColors(EstadoColmena? estado)
    {
        return estado switch
        {
            EstadoColmena.OPTIMO => ("bg-green-100 dark:bg-green-900/50", "text-accent-green dark:text-green-300", "bg-accent-green"),
            EstadoColmena.NECESITA_REVISION => ("bg-orange-100 dark:bg-orange-900/50", "text-accent-orange dark:text-orange-300", "bg-accent-orange"),
            EstadoColmena.EN_PELIGRO => ("bg-red-100 dark:bg-red-900/50", "text-accent-red dark:text-red-300", "bg-accent-red"),
            _ => ("bg-gray-200 dark:bg-gray-600", "text-gray-800 dark:text-gray-300", "bg-gray-500")
        };
    }

    string GetEstadoTexto(EstadoColmena? estado)
    {
        return estado switch
        {
            EstadoColmena.OPTIMO => "Óptimo",
            EstadoColmena.NECESITA_REVISION => "Necesita Revisión",
            EstadoColmena.EN_PELIGRO => "En Peligro",
            _ => "Desconocido"
        };
    }
}
